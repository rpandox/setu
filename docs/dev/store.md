# Store

How Setu persists hosts, snippets, runbooks, and settings as plain TOML in
`~/.config/setu/`, and why that directory is the sync unit.

**Stub — filled in by Phase 2**, when `hosts.toml` CRUD lands in
[`src-tauri/src/store.rs`](../../src-tauri/src/store.rs). Schemas are
specified in [PLAN.md](../../PLAN.md) §4.
